
After do some chnage, execute the following commands to lint and format the codebase, as well as to run tests:

```bash
uv run task lint # Verify code style and formatting without making changes
uv run task fix # Automatically fix code style and formatting issues
uv run task test # Run the test suite to ensure everything is working correctly
```